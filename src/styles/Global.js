import { createGlobalStyle } from 'styled-components';

const Global = createGlobalStyle`
   body {
       background: linear-gradient(180deg, rgba(199,24,53,1) 0%, rgba(199,21,133,1) 53%, rgba(96,0,64,1) 99%);
       height: 100vh;
   }
`;

export default Global;